# Poison fixture signer-role table (tag-roles-derived). PLACEHOLDER until make-fixture.sh
# runs at the sitting: it rewrites this file with the trust-root pin of the bundle it
# generates. Run against this placeholder, the fixture fails for the wrong reason.
version: 1
trust_root:
  tag: brief-freeze
  object: "0000000000000000000000000000000000000000"
  signer: owner
roles:
  - {pattern: brief-freeze, signer: owner}
  - {pattern: "*-laws-freeze", signer: builder}
  - {pattern: "*-close", signer: owner}
  - {pattern: "amendment-*", signer: owner}
  - {pattern: "receipt-*", signer: owner}
# Derived, never enumerated (D0205). Empty, as the real table is.
required_tags: []
unknown: refuse
