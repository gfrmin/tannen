# M4 boundary sitting — the owner's agenda

Generated from `boundary_sitting.sh`, sha256 `c88d3b9…` — **NOT CLEARED.** No full, non-FAST rehearsal has run green over these bytes; FAST rehearsals skip both `make verify` legs and check_decisions.

**19 steps. 18 prompts state a cost for declining, 7 do not.** A prompt with no `else` arm simply skips its action — usually a printout, but NOT always: step 0's `make verify` has no else arm and declining it skips the precondition gate. Not every site fires — several sit inside conditionals, so the number you meet depends on repo state.

**Step 8 will ask you 14 times**, once per record still blocked-on-owner at any tier — derived from `decisions/` just now: D0214 (Tier C), D0215 (Tier C), D0217 (Tier A), D0228 (Tier C), D0229 (Tier A), D0238 (Tier C), D0240 (Tier C), D0241 (Tier A), D0242 (Tier C), D0244 (Tier C), D0247 (Tier A), D0248 (Tier A), D0249 (Tier A), D0250 (Tier A).

**8 steps do something consequential**, and only these:

- **Step 1b** — uses your key
- **Step 2** — uses your key
- **Step 7** — uses your key
- **Step 8** — uses your key
- **Step 8b** — uses your key
- **Step 9** — signs the receipt, writes a commit
- **Step 10** — mints the close tag
- **Step 11** — writes a commit

Everything else edits files you can revert. Every prompt is decline-and-continue; the sitting stops without a close tag only if you decline step 9's commit or step 10's tag, or if step 10 finds a red-team finding the ledger does not account for.

---

## Step 0 — boundary sitting, $MILESTONE. Precondition: a green gate and a read queue

- *no stated consequence* — Show what is already changed? — **n** skips the action (**y** runs `git status --short | page`).
- *no stated consequence* — Run 'make verify' now (recommended — nothing should be signed over a red gate)? — **n** skips the action (**y** runs `if [ "$FAST" = 1 ]; then`).
- *no stated consequence* — Show the owner queue (ROADMAP.md §3 — every record blocked-on-owner, at any tier)? — **n** skips the action (**y** runs `sed -n '/^## 3\. /,/^## 4\. /p' ROADMAP.md | page`).
- *no stated consequence* — Start an ssh-agent for this sitting (stopped again when the driver exits)? — **n** skips the action (**y** runs `if eval "$(ssh-agent -s)" >/dev/null && [ -n "${SSH_AGENT_PID:-}" ]; t`).
- *no stated consequence* — ssh-add $OWNER_KEY now? — **n** skips the action (**y** runs `if ssh-add "$OWNER_KEY"; then`).

## Step 1 — the shell's IO doorway is a hand-written list, and subprocess is not on it

*Resolves: RT-M4-02, D0229 item 2, D0248*


**Keep the doorway guard, its contract, and their custody and gate rows?**

Answering **n**:

> reverted — all of it. RT-M4-02 stays live: make verify green still does not mean
> no IO, and its fixture is not offered at step 5.


## Step 1b — Tier-C records already recorded accepted (a safety net; usually empty)  ⚠ uses your key

No prompt — the driver acts or skips on its own.

## Step 2 — policy.yaml says SpendGuard enforces the envelope, and it does not  ⚠ uses your key

*Resolves: D0240 item 1, D0238*


**Keep the corrected sentence (policy.yaml is re-signed next)?**

Answering **n**:

> reverted — policy.yaml goes on claiming what SpendGuard does not do, and a
> precondition to the first spend stays open (D0243 reports it as policy-sentence).


## Step 3 — check_manifest compares ceilings with the largest envelope, and locks nothing

*Resolves: D0240 item 3, D0244 item 1, D0229 item 5*


**Keep both changes to check_manifest.py (current envelope + pre-budget lock)?**

Answering **n**:

> reverted — the max-over-all-envelopes comparison stays, the lock stays a test file
> a builder can delete, and two preconditions to the first spend stay open.


**Keep the shape row that pins shell-no-subprocess to the whole package?**

Answering **n**:

> reverted — check_manifest skips unknown contract ids, so shell-no-subprocess can be
> narrowed to one module with every guard green.


## Step 4 — required_tags lags one milestone at every boundary, for the fifth time

*Resolves: D0205, D0229 item 4*


**Keep the derived required_tags (guard, tag-roles.yaml, tests, fixture)?**

Answering **n**:

> reverted — all four files. The enumeration stays, and so does step 11's reason to exist.

- *no stated consequence* — Add m4-laws-freeze to the hand-maintained required_tags instead? — **n** skips the action (**y** runs `python3 - <<'PY' || die "tag-roles.yaml's required_tags is not the exp`).

## Step 4b — minting the close tag reddens the commit it attests

*Resolves: D0215*


**Keep the clock verdict out of DECISIONS.md (generator, guard and tests)?**

Answering **n**:

> reverted, and DECISIONS.md regenerated in the old shape. Step 11 will run, and the
> close tag will again name a commit that fails the gate once minted.


## Step 4c — ROADMAP.md is checked on every commit and regenerated by nothing

*Resolves: D0217 items 1 and 2*


**Keep the roadmap hook and make target?**

Answering **n**:

> reverted — ROADMAP.md stays stale after every record until someone types the command.


## Step 4d — a frozen file carries an unexpanded shell variable

*Resolves: D0229 item 1*


**Keep the corrected founding-ratification line (frozen; manifest row refreshed)?**

Answering **n**:

> reverted — the frozen file keeps its literal \$marker.


## Step 4e — two installed fixtures' prose recommends a fix that was measured and is not the fix

*Resolves: D0229 item 3, D0204*


**Keep the corrected fixture prose (manifest rows refreshed)?**

Answering **n**:

> reverted — the fixtures keep recommending the measured non-fix.


## Step 5 — install the poison fixtures (author-key territory)


**Install $fixture into tests/poison/?**

Answering **n**:

> declined — $fixture stays a candidate, and step 6 leaves out its poison line.


## Step 6 — the custodian itself (trust root; yours alone to apply)


**Install this custodian?**

Answering **n**:

> declined — the installed fixtures stay unwatched by the floor, and the custodian's
> completeness pass will say so at step 7.


## Step 7 — sign the custody set  ⚠ uses your key

*Resolves: D0061: what MANIFEST.sha256 cannot be*

- *no stated consequence* — Review the custody set before signing it? — **n** skips the action (**y** runs `page governance/custody.sha256`).

## Step 8 — the records this sitting resolves (ROADMAP.md §3, at any tier)  ⚠ uses your key


**Accept $rec_id (resolved by this sitting)?**

Answering **n**:

> left blocked — it stays in ROADMAP.md §3 for the next sitting


**Keep these binding upgrades on $rec_id (earned by what landed above)?**

Answering **n**:

> upgrades reverted; the record is accepted with the bindings it had


## Step 8b — nine binding upgrades that needed the key that had just left the room  ⚠ uses your key

*Resolves: D0214*


**Keep $rec_id's upgrades and re-sign it?**

Answering **n**:

> reverted — $rec_id keeps bindings that understate their enforcement.


## Step 8c — no standing finding goes unaccounted for

*Resolves: RT-M4-07, D0249*


**These dispositions stand as drafted (n opens the ledger in your editor)?**

Answering **n**:

> re-checked: still complete — the edited ledger's hash goes into the close tag


**Open the ledger now to disposition them?**

Answering **n**:

> left incomplete — step 9 proceeds, and step 10 refuses to close (RT-M4-07)


## Step 9 — regenerate, verify, commit, and take the attention receipt  ⚠ signs the receipt, writes a commit


**Commit the sitting — this is also what takes your attention receipt?**

Answering **n**:

> declined — nothing committed and NO receipt taken (D0201 ruling 4). Step 10 will
> refuse to tag over the uncommitted work.


## Step 10 — sign $MILESTONE-close, LAST  ⚠ mints the close tag


**Sign $MILESTONE-close with this message?**

Answering **n**:

> declined — no close tag. The sitting's commit and receipt stand; re-run to close.


## Step 11 — only if the clock verdict is still in DECISIONS.md  ⚠ writes a commit

No prompt — the driver acts or skips on its own.

