# Poison constitution excerpt — the ONE BRIEF §2-shaped row this fixture's record answers to

A fixture violates a guard; it does not duplicate the governance corpus. This file is not
BRIEF.md and quotes nothing from it: one synthetic row naming TWO owners, so that a record
citing only one — with no `brief_row_divergence` declaration — trips tooth 2 of
`scripts/check_concepts.py` (D0180, patch 05) and nothing else. The guard reads the
constitution at the root it is pointed at and never falls back to the real one, which is
why this excerpt has to exist.

| Concept | Owner(s) | Posture |
|---|---|---|
| Poison concept (`two owners`) | `pkm` POISON-SPEC §1 and `proplang` POISON-DOC §2 | **Adopt.** |
